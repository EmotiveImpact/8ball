# Decision 0002: optional hosted development and a black operator workspace

Date: 23 September 2026. Scope: the owner's request to prepare Hugging Face support for a later API key, continue reliable source-to-plan work and design the software in black.

8BALL remains the fixer product, powered by ENDSTATE. Keep the local/manual planner operational and preserve the existing model failures. Hugging Face is an optional, explicitly configured development provider, not a mandatory backend or evidence that a stronger model has already passed. Hosted inference needs a server-side token, an explicit model/provider and per-analysis transmission permission. No silent paid retries, automatic routing or model download.

Staged draft frames must distinguish the desired outcome from source reports of the existing problem. Bind success criteria to exact target quotations and validate existing-condition references before generating routes. Preserve the original legacy compiler input contract and four real-model fixtures. These are tighter structural/review gates, not proof of semantic correctness.

The interface should use a black canvas, graphite surfaces, readable neutral typography, white primary controls and restrained semantic status colours. Keep the existing case workflows and navigation; do not start another industry application. Display ENDSTATE as the engine identity and show local/runtime/configuration status accurately. Expose the workspace lock on mobile as well as desktop.

Delivery is unpushed in this session because the GitHub write request was blocked. A patch and exact source checkpoint preserve the work against remote base `824ca6421ff3100e1af596347f2d1247dde8f8dc`. Do not imply a main merge, deployment, hosted connection, completed model-quality evaluation or current native CI pass.
