# Current closeout: Source Desk native correction, 26 September 2026

Alpha.8 is now published on `feat/situation-intelligence-v2` at `d27ad600342245d0222b5f4573da8fdcbbf8c1d4`. Read `sessions/2026-09-26-source-desk-native-closeout.md` and `docs/evidence/source-desk-native-closeout.json`. This fix is over that exact remote parent, not a reconstruction to force-push. Previous local-only handoffs are history.

Native run 36217951370 failed only browser-sources. The query refresh race is corrected with reproducible response-order tests. Local code: 938 passed; Source Desk bridge: 38 passed. Patched native verification remains pending until a new GitHub result is recorded. No real inference or independent/human review is implied.

Finish the frozen V0.2/ENDSTATE core scope. Do not import the separate deep research, start another optional screen or merge into main until required acceptance and authority are satisfied. Keep all original gates, changelog history and 71 registered discoveries. This sprint revalidates EM-035/EM-053/EM-051; the detailed findings are retained in the session/changelog.

---

# Current handoff: Chosen Course recovery, alpha.8

Read `docs/v2/CHOSEN-COURSE.md` and `docs/delivery/sessions/2026-09-25-chosen-course-recovery.md`. The recovered local base is `ba0a84a54a0e115ac35b9590c8dc41c931f3e123`, exact tree `907dc4ba85507de1ba1050e69425b3f926d611ac`. It contains alpha.7 and PRD 1.3; later narrated Draft Repair code was not recovered. This sprint reimplements and tests Chosen Course instead of inheriting earlier claimed results.

Run `python scripts/acceptance.py --mode native --base <starting-commit>` through the authorised environment. The explicit bridge is not native acceptance. The new `tests/browser_courses.py` is part of the cumulative eleven-suite runner. Preserve all original model gates, optional full-course dependencies and 71 retained discoveries. No source push, merge or deployment was performed.

Use the complete source or a single patch matched to its declared base. A local self-contained Git bundle is recovery history, not a branch descended from the remote. Do not force-push it over GitHub. Final manifest, commit/tree IDs, patches and actual aggregate reports are in the developer pack.

---

# Current handoff: vision-strengthening documentation overlay

Read `docs/delivery/sessions/2026-09-24-vision-strengthening.md` and ADR 0004 first. This checkpoint updates PRDs, roadmap, prompts and the complete emergence register over the recovered alpha.7 source tree `5c49a40395e6d22510a8895a90ddd2f759378efa`. No runtime code or required task status changed. No remote read, source push, merge, deployment or new model inference is claimed by this documentation review.

The latest supplied Draft Repair handoff explicitly says its completed working tree was not recovered. That record is preserved in `docs/reference/2026-09-24-draft-repair-delivery-record.md`; its reported 905-test total is not evidence for this source. Resolve ES03-08 before claiming that implementation is present. The previous alpha.7 handoff below remains historical implementation context, not a current remote status refresh.

The new optional design/recovery tasks are B03-07, ES02-06, ES03-08 and B04-04. The emergence register now preserves 65 findings. Integrate this patch only against its exact source base; reconcile newer source, histories and any independently allocated task/insight IDs rather than overwriting them.

---

# Current handoff: integrated acceptance, alpha.7

Read `docs/v2/ACCEPTANCE-RUNNER.md`, `docs/v2/ACCESSIBILITY.md`, `docs/endstate/PACKAGE.md` and `docs/delivery/sessions/2026-09-24-integration-acceptance-local.md` first. This complete local `0.2.0-alpha.7` checkpoint includes every previous upgrade. Local input tree `cc821a2c5bfba0aa4027dcdc435d93a33ba5670c`, comparison commit `61ffde6b5b98165d376d0f88345c752d784b5659`. Remote inspected head remains `824ca6421ff3100e1af596347f2d1247dde8f8dc`; no source push, merge or deployment is claimed.

Use `python scripts/acceptance.py --mode native --base <starting-sha>` in an authorised environment with development dependencies and Chromium. A blocked native probe is not a pass and must not be replaced silently by a bridge. The full source/patch includes the same runner used by the configured CI. Exported reports validate their source and artifacts using `--inspect`; hash consistency is not an independent security attestation.

ENDSTATE's nested result contracts and an actual internal wheel are implemented and tested. The installer uses no package index/model download, and can rerun all code tests against the installed kernel. Do not upload the distribution publicly. The real-provider runner is separate, explicit and leaves failures visible. Its scripted judgements are not independent human labels. No token belongs in chat, source, a command argument or a public artifact.

Keep all old gates, changelog entries and 55 retained discoveries. Update the canonical ledger, not parallel task lists. This is a consolidation sprint within V0.2, not a claim that all V0.2 or agency-security requirements are finished.

---

## Previous handoffs (historical, retained)

# Current handoff: Plan Review, alpha.6

Read `docs/v2/PLAN-REVIEW.md`, `evals/PLAN-QUALITY-GATE.md`, and `docs/delivery/sessions/2026-09-24-plan-review-local.md`. This complete local `0.2.0-alpha.6` source includes Source Clarity alpha.5 and all previous unpublished upgrades. Input source tree: `c057d35ff84c1480243208d9674f4e99f7bf74a6`. This sprint's local comparison commit is `ae6e2ac246b44309197aed6b7a047d8a88e10afa`.

GitHub integration remains `feat/situation-intelligence-v2`, PR #2, last inspected head `824ca6421ff3100e1af596347f2d1247dde8f8dc`. Refresh it before integration. No source write, merge, deployment or live provider inference is claimed for this checkpoint. Do not apply older cumulative patches on top of this complete package.

Current work: B02-11 plan-quality review infrastructure plus B02-13 guided interface and B02-03 route-identity correction. Six-part human review records are not action approvals or fact attestations. Scenarios reset to one baseline; the review excludes unsaved Plan Studio changes. The quality protocol and unfilled worksheet do not constitute independent professional review.

Run all code/document checks and all nine native browser scripts where allowed; explicitly bridged results are not native acceptance. Updated test counts are in `docs/evidence/plan-review-local-verification.json`. Keep every discovery, including deferred EM-048. Next task selection: B02-09/B02-11/B02-12/B02-16 and ES01-05, according to their unchanged acceptance dependencies.

---

## Previous handoff (historical, preserved)

# Current handoff: Source clarity, alpha.5

Start with `docs/v2/SOURCE-CLARITY.md` and `docs/delivery/sessions/2026-09-24-source-clarity-local.md`. This is the complete local `0.2.0-alpha.5` source, including all preceding unpublished builds. The input was the Emergent Insights alpha.4 archive, exact tree `a6cdfbf32e2b5d7a9803b9de94a3b729d847c1f6`; the local comparison baseline is `7d6e144a8450cea090cc832ede44a2b3a5857ac0`, not a pushed GitHub commit.

Remote integration remains `feat/situation-intelligence-v2`, PR #2, inspected head `824ca6421ff3100e1af596347f2d1247dde8f8dc`, draft/unmerged. Use the complete source or one matching cumulative/incremental patch. Do not stack cumulative patches, force-push, overwrite newer Codex work or route around the prior source-write denial. Inspect and reconcile the remote before authorised integration.

Run all automated tests, generated-document guards and all **eight** native browser suites, including `tests/browser_grounding.py`. Bridge results do not establish native HTTP/storage/download/CSP behaviour. Record all identified discoveries with appended EM-nnn decisions; update the canonical task ledger and immutable changelog every sprint. This sprint adds EM-038 to EM-043, with business-calendar semantics deliberately retained but deferred.

Keep B02-10/B02-14 partial and B02-09’s real-model gate unchanged. This workflow is human interpretation, not autonomous extraction or actor merging. Review the six new discoveries, run independent source/identity/date fixtures and improve existing gates before moving to V0.3 production-security work.

---

## Preserved prior handoff instructions

# Shared handoff: this chat and Codex

## Newest checkpoint: Source Desk, 0.2.0-alpha.3

Start with the latest Source Desk source/patch. It includes every previous local checkpoint, including Plan Studio. Remote ancestor remains `824ca6421ff3100e1af596347f2d1247dde8f8dc` at inspection. Read `sessions/2026-09-24-source-desk-local.md` and `docs/v2/SOURCE-DESK.md`. Do not apply older cumulative patches twice. Run all code tests and add `tests/browser_sources.py` to the complete native regression gate. No new native CI or live-model result is claimed. B02-14 is partial because identity/claim reconciliation and independent review are still unfinished.

The source-write block has not been bypassed. Record successful integration, actual commit and native CI only after observing them. Maintain the canonical task ledger and append the changelog for every new build. No merge or deployment is authorised.

## Previous local checkpoint: Plan Studio + controlled analysis + changelog

This **local-only** `0.2.0-alpha.2` build includes every previous HF/black/relationship-explorer change plus guided plan amendments, read-only preview, cancellable local analysis jobs and an in-app changelog. Remote ancestor: `824ca6421ff3100e1af596347f2d1247dde8f8dc`. Read `sessions/2026-09-23-plan-studio-local.md` first. Do not apply older cumulative patches on top of this one.

Every subsequent build MUST append `docs/delivery/changelog.json`, regenerate `CHANGELOG.md`, update the sole task ledger, and run both `scripts/changelog.py --check --base <starting-sha>` and `scripts/delivery.py --check`. Add evidence and honest limitations. Existing changelog entries from that base are immutable; corrections and integration results get a new entry. Run the new `tests/browser_studio.py` alongside all earlier browser journeys. Local bridge results do not establish native browser operation. No merge or deployment is authorised.

## Historical integration context (newest local checkpoint above takes precedence)

The product is **8BALL for fixers**, powered by the reusable **ENDSTATE** engine. Read `AGENTS.md`, `docs/PRD.md`, `docs/ROADMAP.md`, this file and `docs/delivery/PROGRESS.md`. The full original 37-section PRD is now in `docs/reference/8BALL-V0.2-ASTRA-PRD.md`; do not rely on a missing chat attachment. Current states belong only in `docs/delivery/progress.json`.

The last remote application-code verification recorded in this historical handoff is `3a33866f375be355658d8e5d82addb25bcc7eed9`, branch `feat/situation-intelligence-v2`, draft PR #2. The staged drafting continuation, 306-test/native evidence and failed actual Qwen result are recorded in `docs/delivery/sessions/2026-09-23-staged-drafting.md` and `docs/endstate/COMPILATION.md`. ENDSTATE extraction evidence is in `docs/endstate/VALIDATION.md` and the contract is in `docs/endstate/CONTRACTS.md`. Refresh GitHub and local Git before starting: this is a checkpoint, not a promise that the remote head never changes. The ledger distinguishes this code baseline from later documentation commits.


## Earlier local patch, not a remote checkpoint

The supplied source/patch contains additional work against `824ca6421ff3100e1af596347f2d1247dde8f8dc`: optional HF transport, target-bound early frame validation, black UI and explicit runtime status. The attempted GitHub write was blocked, so this delta is **not on the integration branch yet**. Read `sessions/2026-09-23-hosted-black-local.md` and apply the delivered patch only after verifying the base and a clean worktree. Its original source tree was verified; do not merge the local reconstructed Git history as though it were the remote branch.

Run the shared ledger check, 362-test suite, compilation and all three browser suites, including `tests/browser_development.py`. Current browser results are explicitly bridged, not new native CI. No HF request was made. Mark the checkpoint pushed only after observing a successful real push, then record the actual commit and CI evidence. Keep original model failures and acceptance criteria.

## Earlier local checkpoint: relationship explorer

The latest supplied `8BALL-Graph-Explorer-Source.zip` / cumulative patch includes BOTH the previously unpublished HF/black work and B02-20. Do not apply the older patch twice. Remote base remains `824ca6421ff3100e1af596347f2d1247dde8f8dc` at inspection. Read `sessions/2026-09-23-relationship-explorer-local.md` and `docs/v2/GRAPH-EXPLORER.md`. Local evidence is 399 pytest and 131 explicitly bridged browser checks. Add `tests/browser_graph.py` to the native regression gate. There is no new live model result or remote commit.

## First session checks

1. Read the current PR/branch, latest commits and relevant CI. Verify the local remote URL and run `git status --short`, `git branch --show-current`, `git rev-parse HEAD` and `git fetch origin`.
2. Do not overwrite a dirty worktree, reset someone else's branch, force-push or assume `main` contains V0.2. Use the repository's current development branch from the ledger. Fast-forward only after reviewing local changes and divergence.
3. Read the specific task, its prerequisites and current implementation before changing code. Choose a bounded unit of work and create a working branch using `work/<task-id>/<session-name>` or the agreed active feature branch.
4. Record owner, working branch, base commit and next action in the task. Push a claim/handoff commit or coordinate on the PR before another session works on the same files. An unpushed local claim is not a distributed lock.

## Current next work, not a fresh rewrite

The first product blocker is `B02-09`: reliable reviewed graph proposals from genuinely new source material. The original captured Qwen graph named an absent verification action. A new explicit two-stage compiler now supplies IDs/wiring from complete model operations, but actual run 35911570117 still failed: readiness/goal duplication, invented existing-condition IDs and target confusion. Both failures are preserved; no template or synthetic verifier fixes the model response. Preserve and reproduce that failure. Do not manufacture the missing action, swap in a catalogue plan, weaken validation or shrink the test set to produce a green result. A typed multi-stage proposal/compiler or a different provider can be evaluated, but both must meet the same declared gate.

`ES01-01` through `ES01-04` now have scoped extraction evidence. 8BALL uses the embedded ENDSTATE kernel, and neutral/sales/support fixtures share that code without breaking old wire or audit records. Continue with `ES01-05`: review the embedded package and versioned result compatibility before accepting the package gate. This does not authorise a framework rewrite, public SDK launch or a new sales application.

`B02-18` stays blocked until the full V0.2 acceptance requirements pass. Optional `ES03-03` Jev live testing is blocked on an authorised key; never request a secret in a repository, PR comment, commit or public handoff.

## During implementation

Use stable task IDs in branch/commit/PR descriptions. Link a shared engine task from an 8BALL task rather than claiming it twice. Record new findings as blockers or explicit new tasks; do not silently rewrite an accepted objective. Keep model interpretation, evidence state and execution authority separate. Keep test fixtures fictional and avoid confidential information in logs/artifacts.

Before pushing, fetch again. If the source moved, inspect the diff and reconcile with normal merge/rebase on your own working branch. Re-run affected tests. Do not push a tree based on an old parent and assume it contains the other environment's changes.

## Status updates and checkboxes

Edit **only `docs/delivery/progress.json` for task states**, then run:

```sh
python scripts/delivery.py --write
python scripts/delivery.py --check
```

`PROGRESS.md` is generated. `[x]` means the named acceptance scope has evidence, not “someone wrote code”. Other states remain unchecked: `not_started`, `in_progress`, `implemented`, `partial`, `blocked`, `deferred`. Checked tasks need evidence references; failed gates need the failure and next action. Do not delete a task to improve completion figures. No percentage of total product readiness is inferred from the number of boxes.

Every substantive task records: ID, status, owner, branch/base commit, code paths, acceptance criterion, evidence IDs, blockers, next action and update date. Store exact code/CI/model artifact references under the ledger evidence registry. Redact secrets and sensitive source material. If a completed task regresses, reopen it and preserve its prior evidence/history in the session record.

## Mandatory Emergence Review

Every substantive build sprint ends by asking:

> **What did this sprint reveal that we had not properly seen, specified or understood before we built it?**

Use `docs/delivery/EMERGENCE-REVIEW.md`. Record new capabilities/opportunities, new risks, ENDSTATE-vs-8BALL architecture implications, operator/UX implications, evidence level, and the roadmap disposition. The default is **not** “add it to scope”: use `existing_task`, `new_candidate`, `new_required_task`, `decision_record` or `no_action`. A required task or scope change needs an explicit product decision and canonical ledger update.

The same review must be represented in the new `docs/delivery/changelog.json` entry. `python scripts/changelog.py --check --base <starting-sha>` rejects future changed-build entries that omit the emergence review.

## End every work session with a reproducible checkpoint

Use the template below in `docs/delivery/sessions/<date>-<session-id>.md`; add a brief PR comment pointing to it. Update affected tasks and regenerate the checklist in the same delivery. A documentation change may be verified using document/tool checks; application changes need the relevant existing and new tests.

```text
Session ID / environment: ChatGPT or Codex
Task IDs / objective:
Branch / base commit:
Files changed:
Implementation commits:
Tests and exact results / evidence:
Real models called? Runtime, model revision, data scope and result:
What remains unverified or failed:
Task status changes:
Emergence Review — what did this sprint reveal that we had not properly seen before?:
Emergence evidence level / roadmap disposition:
Last pushed commit / PR:
Unpushed work or patch/bundle location:
Next task and exact next command/test:
Merge/deployment state:
```

A commit cannot contain its own final hash: record its base/tested code revision inside the commit, then put the resulting commit ID in the PR handoff comment or the next checkpoint. Do not fabricate a self-referential hash or continually amend documentation to chase one.

When work cannot be pushed, provide an exact patch/bundle or source archive plus base commit and checksums. Clearly say it is not on the remote yet. The next environment cannot see an unpublished container directory or uncommitted local code.

## Test and release rules

Run the original and new backend tests, Python compilation, browser JavaScript syntax checks and appropriate native browser journeys for changed behaviour. Label bridge, mock, live model and native runs accurately. Read the actual CI result; starting a workflow is not a pass. Documentation tracking is checked by `scripts/delivery.py` and its dedicated workflow.

No automatic main merge, draft-to-ready conversion or deployment is authorised by this handoff. Re-evaluate current reviews and gates before asking for the release decision. Keep both product readiness and ENDSTATE packaging readiness distinct. A tested developer baseline can exist without claiming the full fixer platform is ready for customers.

## Copyable continuation instruction

Continue EmotiveImpact/8ball. Read AGENTS.md, docs/PRD.md, docs/ROADMAP.md, docs/delivery/PROGRESS.md and docs/delivery/HANDOFF.md. Confirm current GitHub and local branch/commit before changing anything. Keep 8BALL as the fixer product and ENDSTATE as its reusable engine. Select the next unblocked task from the ledger, preserve all evidence/approval/audit invariants, add and run tests, update task evidence and checkboxes, push a reproducible checkpoint and record unfinished work. Do not rewrite the application, silently change acceptance gates, merge or deploy without an explicit decision.

## Current accepted addition: product emergence and a complete owner register

The owner has requested both case-level **Emergent Insights** and an evolving record of **all build discoveries**, including ideas not selected by the developer. Keep these separate. The first bounded implementation is tracked by B02-21 / ES02-05, its usefulness/native acceptance by B02-22, and the enduring register/prompt workflow by DOC-06. Existing source-to-plan and production gates remain open.

Every handoff must list the `EM-nnn` discovery IDs considered, implemented, deferred or declined, the evidence level and the reason. Use `docs/prompts/EMERGENT-INSIGHTS.md` and the generated `docs/delivery/EMERGENCE-REGISTER.md`. Edit the JSON register and append decisions; do not remove discoveries or rewrite original observations. Include a clear owner-review queue. Run `python scripts/emergence.py --check --base <starting-sha>` in addition to the existing delivery/changelog checks. This is an evolving register, not a promise to implement every idea.

For the Emergent Insights feature, keep model state, review disposition and present/historical context independent. A useful or review-complete finding is not a confirmed fact. A later scan must not silently inherit a dismissal after changed content or a disappearance/reappearance. Store reviewer reasons and the exact inspected snapshot/fingerprint. Test concurrency, stale context, authentication, case isolation, atomic question creation, exports and the black desktop/mobile workflow.
