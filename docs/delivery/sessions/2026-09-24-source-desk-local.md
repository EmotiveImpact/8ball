# Source Desk local continuation

**Environment:** ChatGPT local development. **Tasks:** B02-14 (primary), B02-10/B02-16/B02-19 supporting scope; mandatory DOC-04 history update.

## Source and delivery boundary

The supplied `8BALL-Plan-Studio-Source.zip` is the starting application. Its reconstructed local tracked tree is `0bb462f227bd93fec2685d8bf81326393eb8f01b`. The original integration branch is still `feat/situation-intelligence-v2`, remote head `824ca6421ff3100e1af596347f2d1247dde8f8dc`, tree `68ff7ac7b212fbdf67f77d051f69d13ed39485be`, draft PR #2 at inspection. This local work uses `work/B02-14/source-desk` and preserves all earlier unpushed provider, black-interface, graph and Plan Studio work. A reconstructed local commit is not remote Git history.

The previous source-write denial was not retried through another route. No new push, native CI, merge or deployment is claimed. Deliver the complete source and cumulative exact-base patch. A fresh remote clone does not contain this source checkpoint.

## Built

A black Source Desk within the existing Evidence area, rather than another top-level application. It stores immutable bounded text originals, exposes complete chunk maps and literal search, maps browser selections to original Unicode positions, previews and captures exact passages, checks duplicates before import, preserves linked versions, traces excerpts and quotes to originals, and retracts originals with their linked evidence atomically.

Full originals stay outside provider context and ordinary case snapshots. Model input limits and the human-attestation boundary remain unchanged. Original descriptors, passage ranges and hashes are anchored in case events. Existing source-free exports still match their historical compatibility fixtures. The body boundary now enforces actual streamed limits and grants a larger allowance only to source import/preview.

## Verification

See `docs/evidence/source-desk-local-verification.json` for the completed results. At this checkpoint, 566 automated tests pass, including 82 new source/API/Unicode-selection tests. All six browser suites passed 210 checks, including 35 Source Desk and 175 preserved/updated regression checks. The source browser journey verifies the real API and SQLite through the explicit bridge; native navigation returned `ERR_BLOCKED_BY_ADMINISTRATOR` and was not bypassed. No model inference took place.

One regression during implementation exposed an additive-field change to old audit export digests. The implementation was corrected to preserve old source-free exports byte-for-byte; the existing compatibility test was not weakened. Another check showed that `Blob.text()` hides a leading BOM in a test read; the export test now checks actual blob bytes, without altering the stored or exported text.

The previous browser test pinned the newest changelog entry to `alpha.2`. It now checks the current version constant and separately checks that the original Plan Studio entry remains present. History was not removed or rewritten.

## Remaining scope

B02-14 advances from not started to partial, not verified. Text source provenance and explicit source duplicates are implemented. Entity/claim merges, semantic duplication, richer binary documents, independent operator review and native integration remain incomplete. B02-09's real-model planning gate stays blocked. B02-10's date/identity/negation quality gate is not solved by storing a longer source.

The next useful build is source-grounded, human-reviewed identity/deadline reconciliation using the preserved originals and exact quote locations. The full real-provider planning journey remains separately required. Use fictional/test data only; V0.3's secure agency requirements remain future work.
